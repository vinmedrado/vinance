import{useState}from'react';import{useNavigate}from'react-router-dom';import{LockKeyhole,Sparkles,WalletCards}from'lucide-react';import{login}from'../services/api';import{Button,Card,Callout,MetricCard}from'../components/ui';
export default function Login(){const navigate=useNavigate();const[email,setEmail]=useState('demo@financeos.local');const[password,setPassword]=useState('financeos123');const[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setError('');try{await login(email,password);navigate('/')}catch{setError('Não foi possível entrar. Confira seus dados ou use o modo demo local.')}}return <div className="auth-page"><div className="auth-hero"><span className="eyebrow">ERP financeiro inteligente</span><h1>Controle despesas, orçamento, metas e investimentos em uma experiência premium.</h1><p>O FinanceOS conecta rotina financeira, diagnóstico e inteligência de investimentos sem expor complexidade técnica ao usuário final.</p><div className="grid two"><MetricCard icon={<WalletCards size={18}/>} label="Fluxo principal" value="ERP" hint="Receitas, despesas e orçamento"/><MetricCard icon={<Sparkles size={18}/>} label="Diferencial" value="IA" hint="Diagnóstico e recomendações"/></div></div><Card className="auth-card"><div className="metric-icon"><LockKeyhole size={19}/></div><h2>Entrar no FinanceOS</h2><p>Acesse seu painel financeiro executivo com visual SaaS premium.</p>{error&&<Callout title="Acesso não concluído" message={error} tone="danger"/>}<form onSubmit={submit} className="form"><label>E-mail<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input value={password} type="password" onChange={e=>setPassword(e.target.value)}/></label><Button>Entrar</Button><Button type="button" variant="ghost" onClick={()=>navigate('/onboarding')}>Ver onboarding</Button></form></Card></div>}
