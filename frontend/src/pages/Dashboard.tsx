import{Area,AreaChart,Bar,BarChart,CartesianGrid,Cell,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';import{ArrowUpRight,PiggyBank,ShieldCheck,TrendingUp,Wallet}from'lucide-react';import{AlertCard,ChartCard,EmptyState,LoadingState,MetricCard,RecommendationCard,SectionHeader}from'../components/ui';import{useApi}from'../hooks/useApi';import{Dashboard as DashboardType}from'../types/finance';import{money,pct}from'../utils/format';
export default function Dashboard(){const{data,isLoading,error}=useApi<DashboardType>(['dashboard'],'/dashboard');if(isLoading)return <div className="page"><LoadingState title="Montando sua visão executiva..." rows={5}/></div>;if(error||!data)return <div className="page"><EmptyState title="Dashboard pronto para começar" message="Cadastre receitas e despesas para liberar score, orçamento, diagnóstico e sugestão de investimento."/></div>;const m=data.metrics;const budgetUsed=data.budget?.monthly_income?Math.min(100,(data.budget.actual.expenses/data.budget.monthly_income)*100):0;const insights=[{title:'Orçamento usado',value:`${budgetUsed.toFixed(0)}%`,text:'Acompanhe se o mês está dentro do plano.'},{title:'Tendência financeira',value:m.monthly_balance>=0?'Positiva':'Atenção',text:m.monthly_balance>=0?'Há sobra disponível para metas e aportes.':'Revise categorias com maior consumo.'},{title:'Próximo passo',value:'Investir',text:`Sugestão atual: ${money(data.recommendation.amount)}.`}];return <div className="page"><SectionHeader eyebrow="Visão executiva" title="Dashboard Financeiro" description="Controle do mês, orçamento, diagnóstico e investimento recomendado em uma experiência executiva." action={<div className="score-pill">Score {m.financial_score}</div>}/><div className="hero-summary"><section className="premium-card hero-balance"><span className="eyebrow">Saldo do mês</span><strong>{money(m.monthly_balance)}</strong><p>{m.monthly_balance>=0?'Seu mês está com sobra. O FinanceOS conecta esse valor ao orçamento, metas e investimentos.':'Seu mês está pressionado. Veja alertas e categorias para corrigir antes do fechamento.'}</p><div className="progress-track"><span className="progress-value" style={{width:`${budgetUsed}%`}}/></div><small>Orçamento utilizado: {budgetUsed.toFixed(0)}%</small></section><section className="premium-card insight-list"><span className="eyebrow">Resumo inteligente</span>{insights.map(i=><div className="insight-item" key={i.title}><div className="metric-icon"><ArrowUpRight size={17}/></div><div><strong>{i.title}: {i.value}</strong><p>{i.text}</p></div></div>)}</section></div><div className="metrics-grid"><MetricCard icon={<Wallet size={18}/>} label="Receitas" value={m.total_income}/><MetricCard icon={<TrendingUp size={18}/>} label="Despesas" value={m.total_expenses} tone="warn"/><MetricCard icon={<PiggyBank size={18}/>} label="% investido" value={pct(m.invested_pct)} hint={money(m.realized_investment)}/><MetricCard icon={<ShieldCheck size={18}/>} label="Economia" value={m.available_to_invest} tone="good"/></div><RecommendationCard title={data.recommendation.title} message={data.recommendation.message} amount={data.recommendation.amount}/><div className="grid two"><ChartCard title="Evolução mensal" hint="Receitas x despesas"><ResponsiveContainer width="100%" height={300}><AreaChart data={data.charts.evolution}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis tickFormatter={v=>`R$ ${Number(v)/1000}k`}/><Tooltip formatter={v=>money(Number(v))}/><Area type="monotone" dataKey="receitas" stroke="currentColor" fill="currentColor" fillOpacity={.16} strokeWidth={3}/><Area type="monotone" dataKey="despesas" stroke="currentColor" fill="currentColor" fillOpacity={.08} strokeWidth={2}/></AreaChart></ResponsiveContainer></ChartCard><ChartCard title="Gastos por categoria" hint="Onde o dinheiro saiu">{data.charts.by_category.length?<ResponsiveContainer width="100%" height={300}><PieChart><Pie data={data.charts.by_category} dataKey="value" nameKey="name" outerRadius={108} innerRadius={58} paddingAngle={3} label>{data.charts.by_category.map((_,i)=><Cell key={i}/>)}</Pie><Tooltip formatter={v=>money(Number(v))}/></PieChart></ResponsiveContainer>:<EmptyState title="Sem despesas no mês" message="Cadastre despesas para visualizar categorias."/>}</ChartCard></div><ChartCard title="Comparativo mensal rápido"><ResponsiveContainer width="100%" height={260}><BarChart data={data.charts.evolution}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><Tooltip formatter={v=>money(Number(v))}/><Bar dataKey="receitas" radius={[10,10,0,0]} fill="currentColor"/><Bar dataKey="despesas" radius={[10,10,0,0]} fill="currentColor" opacity={.45}/></BarChart></ResponsiveContainer></ChartCard><div className="grid three">{data.alerts.map(a=><AlertCard key={a.title} title={a.title} message={a.message} tone={a.severity}/>)}</div></div>}
