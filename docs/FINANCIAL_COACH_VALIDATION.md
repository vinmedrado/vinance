# Validação — Vinance Financial Coach

## Executado

- `python -m compileall -q .`: OK
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -q tests/test_financial_coach.py`: 6 passed

## Escopo validado

- score financeiro
- mudança automática de modelo
- coaching contextual
- comportamento financeiro
- forecast financeiro
- timeline financeira

## Limitações

- `npm run build` não foi executado no ambiente atual.
- `docker compose config` não foi executado por indisponibilidade de Docker.
- O forecast usa hipóteses simplificadas, adequadas para educação financeira, não para promessa de retorno.
